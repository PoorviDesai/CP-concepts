The naive approach requires time complexity of O(n)....but using the property:
all divisors of a prime number N occur in pairs of (a, b) such that a * b = N..
so the time complexity becomes O(sqrt(n))
